---
title: "Massive Hermes Chunking Test"
type: wiki
category: architecture
source_raw: RAW/architecture/massive-hermes-chunking-test.md
compiled_at: "2026-05-10 06:39:15"
---

# "Massive Hermes Chunking Test"

## Summary

This WIKI note was compiled from a RAW source by the Hermes-native memory profile.

## Key Knowledge


# Massive Hermes Chunking Test

## Deterministic Summary Placeholder

- This note was generated deterministically from `/tmp/massive-hermes-chunking-test.md`.
- No LLM summary has been generated yet.
- Source chunk count: 3.

## Merged Key Content

### Source Chunk 01

# Massive Hermes Chunking Test

## Overview

This is an intentionally large document to test native Hermes Memory chunking.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

### Source Chunk 02

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

### Source Chunk 03

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Repeated Architecture Section

Hermes-native memory should support long transcript ingestion, chunk splitting, chunk merging, canonical selection, cleanup flows, Git synchronization, Obsidian synchronization, MCP-native retrieval, low-value detection, archive workflows, deterministic pipelines, and explicit optional LLM usage.

Workspace should become the operational cockpit for all AiAgentNerd agents. Memory should become the durable knowledge subsystem. MCP tools should expose safe executable capabilities. The wiki should remain the durable RAW and WIKI substrate.

## Source Chunk References

- [[RAW/source_chunks/massive-hermes-chunking-test/massive-hermes-chunking-test-chunk-01.md]]
- [[RAW/source_chunks/massive-hermes-chunking-test/massive-hermes-chunking-test-chunk-02.md]]
- [[RAW/source_chunks/massive-hermes-chunking-test/massive-hermes-chunking-test-chunk-03.md]]

## Source

- RAW: [[RAW/architecture/massive-hermes-chunking-test.md]]

## Tags

#aiagentnerd #hermes #memory #wiki
